function sum(x,y){
    return x+y;
}
//anonymous function
const mutiple = (x,y) =>{
    return x*y;
}

const substract = (x,y) => x-y; //one line - one function
//------Cho phep functions duoc su dung o ben file js khac
export{
    sum,
    mutiple,
    substract,
}